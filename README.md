# 🎨 DASP Project - Чистая SCSS структура

## 📁 Структура проекта

```
dasp-project/
├── scss/                          # 📦 SCSS исходники
│   ├── _variables.scss            # 🎨 Переменные (цвета, шрифты, размеры)
│   ├── _mixins.scss              # ⚙️ Миксины (медиа-запросы)
│   ├── _common.scss              # 🏗️ Общие стили (reset, базовые стили)
│   ├── _header.scss              # 🏠 Шапка сайта (пустая)
│   ├── _footer.scss              # 🦶 Подвал сайта (пустая)
│   ├── pages/                    # 📄 Стили страниц
│   │   ├── _home.scss            # 🏠 Главная страница (пустая)
│   │   ├── _about.scss           # 👥 О нас (пустая)
│   │   ├── _blog.scss            # 📝 Блог (пустая)
│   │   └── _blog-detail.scss     # 📖 Детальная страница блога (пустая)
│   └── main.scss                 # 🎯 Главный файл
├── css/                          # 📦 Скомпилированные CSS файлы
│   └── main.css                  # 🎯 Главный CSS файл
├── fonts/                        # 🔤 Шрифты
├── img/                          # 🖼️ Изображения
├── js/                           # 📜 JavaScript файлы
├── index.html                    # 🏠 Главная страница (базовая структура)
├── about.html                    # 👥 О нас (базовая структура)
├── blog.html                     # 📝 Блог (базовая структура)
├── blog-detail.html              # 📖 Детальная страница блога (базовая структура)
├── package.json                  # ⚙️ Конфигурация npm
└── README.md                     # 📖 Документация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Разработка с автокомпиляцией
```bash
npm run dev
```
**Этот скрипт автоматически компилирует SCSS при каждом изменении файлов!**

### 3. Сборка для продакшена
```bash
npm run build
```

## ⚡ Автоматическая компиляция

После запуска `npm run dev`:
- ✅ **Автоматически компилирует** SCSS при каждом сохранении
- ✅ **Создает source maps** для отладки
- ✅ **Развернутый CSS** для удобства разработки
- ✅ **Работает в фоне** - не нужно перезапускать

**Просто сохраните SCSS файл и CSS обновится автоматически!**

## 📋 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm install` | Установка зависимостей |
| `npm run dev` | Разработка с автокомпиляцией (автоматически компилирует при изменениях) |
| `npm run build-css` | Однократная компиляция |
| `npm run build` | Сборка для продакшена |
| `npm run clean` | Очистка скомпилированных файлов |

## 🎯 Что готово

### ✅ **Базовая структура**
- **Переменные** - цвета, шрифты, размеры, брейкпоинты
- **Миксины** - медиа-запросы, контейнер, подключение шрифтов
- **Общие стили** - reset, базовые стили, подключение шрифтов
- **Пустые файлы** - готовые для ваших стилей

### ✅ **HTML структура**
- **4 HTML страницы** с базовой структурой
- **Навигация** между страницами
- **Подключение CSS** файла

### ✅ **Готовые миксины**
```scss
// Медиа-запросы
@include mobile { /* стили для мобильных */ }
@include tablet { /* стили для планшетов */ }
@include desktop { /* стили для десктопа */ }
@include large-desktop { /* стили для больших экранов */ }

// Контейнер
@include container; // автоматический контейнер с отступами
```

### ✅ **Современный синтаксис @use**
Все файлы используют современный синтаксис `@use` вместо устаревшего `@import`:
```scss
@use 'variables' as *;
@use 'mixins' as *;
```

### ✅ **Переменные**
```scss
// Цвета
$color-black: #080808;
$color-gray: #666666;
$color-light-gray: #707070;
$color-white: #ffffff;

// Шрифты
$font-title: 'SportingGrotesque', sans-serif;
$font-main: 'Inter', sans-serif;

// Размеры
$container-width: 1840px;

// Брейкпоинты
$breakpoint-sm: 768px;
$breakpoint-md: 1024px;
$breakpoint-lg: 1200px;
$breakpoint-xl: 1440px;
```

## 🔧 Где писать стили

### **Шапка сайта**
```scss
// scss/_header.scss
.header {
  // Ваши стили здесь
}
```

### **Подвал сайта**
```scss
// scss/_footer.scss
.footer {
  // Ваши стили здесь
}
```

### **Страницы**
```scss
// scss/pages/_home.scss
// Ваши стили для главной страницы здесь

// scss/pages/_about.scss
// Ваши стили для страницы "О нас" здесь

// scss/pages/_blog.scss
// Ваши стили для страницы блога здесь

// scss/pages/_blog-detail.scss
// Ваши стили для детальной страницы блога здесь
```

### **Общие стили**
```scss
// scss/_common.scss
// Добавьте сюда общие компоненты, утилиты, кнопки и т.д.
```

## 🌐 Перенос на WordPress

### 1. **Подготовка**
```bash
npm run build
```

### 2. **Копирование файлов**
- Скопируйте папку `scss/` в тему WordPress
- Скопируйте `css/main.css` в папку темы

### 3. **Подключение в WordPress**
```php
// В functions.php
function enqueue_main_styles() {
    wp_enqueue_style(
        'main-style', 
        get_template_directory_uri() . '/css/main.css',
        array(),
        '1.0.0'
    );
}
add_action('wp_enqueue_scripts', 'enqueue_main_styles');
```

## 🎨 Примеры использования

### Миксины
```scss
// Контейнер
.my-section {
  @include container;
}

// Медиа-запросы
.responsive-text {
  font-size: 1rem;
  
  @include tablet {
    font-size: 1.25rem;
  }
  
  @include desktop {
    font-size: 1.5rem;
  }
}
```

### HTML структура
```html
<!-- Базовая структура уже готова -->
<header class="header">
  <div class="header__container">
    <a href="index.html" class="header__logo">DASP</a>
    <nav class="header__nav">
      <!-- Навигация -->
    </nav>
  </div>
</header>

<main>
  <!-- Ваш контент здесь -->
</main>

<footer class="footer">
  <div class="footer__container">
    <!-- Ваш контент футера здесь -->
  </div>
</footer>
```

## 🏆 Преимущества чистой структуры

✅ **Готовность** - базовая структура настроена  
✅ **Простота** - только необходимые файлы  
✅ **Гибкость** - пишите стили как хотите  
✅ **Масштабируемость** - легко добавлять новые страницы  
✅ **Готовность к WordPress** - легко перенести  
✅ **Современность** - актуальные SCSS возможности  
✅ **Современный синтаксис** - использует @use вместо устаревшего @import  

---

**🎉 Готово!** Чистая SCSS структура готова для вашей работы!